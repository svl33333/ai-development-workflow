# Prototype design

Codexはconcept-briefのパスだけをChatGPTへ渡し、prototypeの詳細設計・評価基準・実装計画を、依存関係と変更スコープを持つ独立実装単位へ分割して受け取る。prototypeも本番計画と同じ最低3ラウンドの設計レビューを行い、承認ダイジェストと最新の提示証跡が一致するまで実装を開始しない。実装は、公開・マージ・親状態変更権限を持たない子タスクから行う。
