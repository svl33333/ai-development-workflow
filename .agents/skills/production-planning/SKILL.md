# Production planning

ChatGPTは承認済みIssue、CONTEXT、ADR、現行コードを読み、承認ダイジェストに束縛された実装計画を作る。計画は、依存関係・変更スコープ・テスト・受入条件・統合条件を持つ独立実装単位へ分割し、`max_parallel_codex_tasks`（初期値3）を明示する。Codexは計画を保存し、最低3ラウンドの独立レビューと改善を完了するまで実装を開始しない。承認後は、各単位を実Git worktreeで実行し、子タスクのローカルレビュー証跡、統合結果、最終テストを保存する。
